# Pardais Party V47 — Exact Logo / Launcher / Splash

Implemented the supplied Pardais Party artwork as the canonical app artwork.

- Exact supplied PNG stored at `public/pardais-party-exact.png`.
- Web/PWA favicon and manifest icons point to the supplied artwork.
- React startup splash uses the supplied artwork on black, with no redesigned logo.
- Pre-React boot screen prevents the blank-screen flash while the app initializes.
- Login/Sign Up gate replaces the old 🎉 marker with the supplied artwork.
- Android legacy launcher icons are regenerated from the supplied artwork.
- Android adaptive foreground preserves the full artwork inside the safe area.
- Native Android splash resources are regenerated from the supplied artwork on black.
- Existing app functionality is otherwise unchanged.

# VIP Frames V51

Replaced the previous generated/programmatic VIP profile frames with the 12 supplied reference artworks, one-to-one from the uploaded sheet.

Assets:
- public/assets/vip-frames/vip-frame-1.svg ... vip-frame-12.svg

The SVG files preserve the supplied artwork via embedded PNG data so the visual artwork remains exact while the application consumes them as SVG assets. The inner avatar opening is transparent so the user's profile photo remains visible.

Existing VIP level/min-level mapping (8,16,...,96) is preserved.

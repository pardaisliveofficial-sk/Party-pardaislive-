// ------------------------------------------------------------------
// GIFTER REVENUE SHARE / INVESTMENT — isolated, additive module
// ------------------------------------------------------------------
const REVENUE_SHARE_COLLECTIONS = ["investment_plans", "investments", "investment_transactions", "investment_earnings", "investment_withdrawals", "revenue_pools", "revenue_distributions"];
function ensureRevenueShareSchema() {
  for (const key of REVENUE_SHARE_COLLECTIONS) if (!Array.isArray(dbData[key])) dbData[key] = [];
  if (dbData.investment_plans.length === 0) {
    dbData.investment_plans.push(
      { id: "bronze", name: "Bronze", minDeposit: 500, maxDeposit: 999, revenueSharePercent: 1, payoutCycle: "Monthly", coinConversionRate: 100, status: "Active", createdAt: new Date().toISOString() },
      { id: "silver", name: "Silver", minDeposit: 1000, maxDeposit: 4999, revenueSharePercent: 1.5, payoutCycle: "Monthly", coinConversionRate: 100, status: "Active", createdAt: new Date().toISOString() },
      { id: "gold", name: "Gold", minDeposit: 5000, maxDeposit: 9999, revenueSharePercent: 2, payoutCycle: "Monthly", coinConversionRate: 100, status: "Active", createdAt: new Date().toISOString() },
      { id: "diamond", name: "Diamond", minDeposit: 10000, maxDeposit: null, revenueSharePercent: 2.5, payoutCycle: "Monthly", coinConversionRate: 100, status: "Active", createdAt: new Date().toISOString() }
    );
  }
}
ensureRevenueShareSchema();
const revenueShareAdminEmail = (req: any) => String(req.body?.adminEmail || req.headers["x-admin-email"] || "").toLowerCase().trim();
const requireRevenueAdmin = (req: any, res: any) => { if (!isAdminEmail(revenueShareAdminEmail(req))) { res.status(403).json({ error: "Authorized admin email is required." }); return false; } return true; };
const persistRevenueRecord = async (collection: string, record: any) => { saveDatabase(); try { await syncDocument(collection, String(record.id), record); } catch (e) { console.warn(`[REVENUE SHARE] ${collection} sync delayed`, e); } };
const investmentOwnerMatch = (inv: any, user: any) => String(inv.userId || "") === String(user?.uid || user?.uniqueId || "") || String(inv.username || "").toLowerCase() === String(user?.username || "").toLowerCase();

app.get("/api/v1/revenue-share/plans", (_req, res) => { ensureRevenueShareSchema(); res.json(dbData.investment_plans.filter((p: any) => p?.status === "Active")); });
app.get("/api/v1/revenue-share/dashboard", authenticateUser, (req: any, res: any) => { ensureRevenueShareSchema(); const own=dbData.investments.filter((i:any)=>investmentOwnerMatch(i,req.user)); const ids=new Set(own.map((i:any)=>i.id)); const month=new Date().toISOString().slice(0,7); const earnings=dbData.investment_earnings.filter((e:any)=>ids.has(e.investmentId)); res.json({ totalDeposit:own.reduce((n:number,i:any)=>n+Number(i.principal||0),0), activeBalance:own.filter((i:any)=>i.status==="Active").reduce((n:number,i:any)=>n+Number(i.principal||0),0), thisMonthEarnings:earnings.filter((e:any)=>String(e.period||"").startsWith(month)).reduce((n:number,e:any)=>n+Number(e.amount||0),0), totalEarnings:own.reduce((n:number,i:any)=>n+Number(i.totalEarnings||0),0), availableEarnings:own.reduce((n:number,i:any)=>n+Number(i.availableEarnings||0),0), pendingEarnings:own.reduce((n:number,i:any)=>n+Number(i.pendingEarnings||0),0) }); });
app.get("/api/v1/revenue-share/investments", authenticateUser, (req:any,res:any)=>{ensureRevenueShareSchema();res.json(dbData.investments.filter((i:any)=>investmentOwnerMatch(i,req.user)).sort((a:any,b:any)=>String(b.createdAt).localeCompare(String(a.createdAt))));});
app.get("/api/v1/revenue-share/withdrawals", authenticateUser, (req:any,res:any)=>{res.json(dbData.investment_withdrawals.filter((w:any)=>String(w.userId)===String(req.user.uid||req.user.uniqueId)||String(w.username).toLowerCase()===String(req.user.username).toLowerCase()));});
app.post("/api/v1/revenue-share/investments",authenticateUser,async(req:any,res:any)=>{ensureRevenueShareSchema();const plan=dbData.investment_plans.find((p:any)=>p.id===String(req.body?.planId)&&p.status==="Active");const amount=Number(req.body?.amount);const paymentMethod=String(req.body?.paymentMethod||"").trim();if(!plan)return res.status(400).json({error:"Selected revenue-share plan is not active."});if(!Number.isFinite(amount)||amount<Number(plan.minDeposit)||(plan.maxDeposit!=null&&amount>Number(plan.maxDeposit)))return res.status(400).json({error:"Investment amount is outside the configured plan limits."});if(!paymentMethod)return res.status(400).json({error:"Payment method is required."});const id=`INV-${Math.floor(100000+Math.random()*900000)}`;if(dbData.investments.some((i:any)=>i.id===id))return res.status(409).json({error:"Investment ID collision. Please retry."});const now=new Date().toISOString();const investment={id,userId:String(req.user.uid||req.user.uniqueId),username:req.user.username,planId:plan.id,planName:plan.name,principal:amount,activeBalance:amount,availableEarnings:0,pendingEarnings:0,totalEarnings:0,monthlyEarnings:0,paymentMethod,paymentStatus:"Pending",status:"Pending",createdAt:now,planSnapshot:{...plan},startDate:null};dbData.investments.unshift(investment);const tx={id:`ITX-${Date.now()}-${crypto.randomBytes(3).toString("hex")}`,type:"deposit",investmentId:id,userId:investment.userId,username:investment.username,amount,currency:"USD",timestamp:now,status:"Pending",source:paymentMethod,destination:"revenue_share_principal"};dbData.investment_transactions.unshift(tx);await persistRevenueRecord("investments",investment);await persistRevenueRecord("investment_transactions",tx);res.status(201).json({success:true,investment});});
app.post("/api/v1/revenue-share/withdrawals",authenticateUser,async(req:any,res:any)=>{ensureRevenueShareSchema();const amount=Number(req.body?.amount);const type=String(req.body?.type||"earnings");const payoutFormat=String(req.body?.payoutFormat||"cash");if(!Number.isFinite(amount)||amount<=0)return res.status(400).json({error:"Enter a valid withdrawal amount."});if(!['earnings','principal'].includes(type)||!['cash','coins'].includes(payoutFormat))return res.status(400).json({error:"Invalid withdrawal request."});const own=dbData.investments.filter((i:any)=>investmentOwnerMatch(i,req.user)&&i.status==="Active");const available=own.reduce((n:number,i:any)=>n+Number(type==="earnings"?i.availableEarnings:i.principal),0);if(amount>available)return res.status(400).json({error:`Insufficient ${type} balance.`});let remaining=amount;for(const inv of own){if(remaining<=0)break;const field=type==="earnings"?"availableEarnings":"principal";const take=Math.min(Number(inv[field]||0),remaining);if(take>0){inv[field]=Number(inv[field]||0)-take;if(type==="principal")inv.activeBalance=Math.max(0,Number(inv.activeBalance||0)-take);remaining-=take;}}const id=`IWD-${Date.now()}-${crypto.randomBytes(3).toString("hex")}`;const now=new Date().toISOString();const w={id,userId:String(req.user.uid||req.user.uniqueId),username:req.user.username,investmentId:own[0]?.id||"",amount,type,payoutFormat,currency:"USD",paymentMethod:String(req.body?.paymentMethod||""),status:"Pending",createdAt:now,adminAction:null,completionDate:null,reserved:true};dbData.investment_withdrawals.unshift(w);const tx={id:`ITX-${Date.now()}-${crypto.randomBytes(3).toString("hex")}`,type:"withdrawal_request",investmentId:w.investmentId,userId:w.userId,username:w.username,amount,currency:"USD",timestamp:now,status:"Pending",source:type,destination:payoutFormat};dbData.investment_transactions.unshift(tx);await persistRevenueRecord("investment_withdrawals",w);await persistRevenueRecord("investment_transactions",tx);for(const inv of own)await persistRevenueRecord("investments",inv);res.status(201).json({success:true,withdrawal:w});});
app.get("/api/v1/admin/revenue-share",(req,res)=>{if(!requireRevenueAdmin(req,res))return;ensureRevenueShareSchema();const active=dbData.investments.filter((i:any)=>i.status==="Active");res.json({plans:dbData.investment_plans,investments:dbData.investments,withdrawals:dbData.investment_withdrawals,revenuePools:dbData.revenue_pools,transactions:dbData.investment_transactions,metrics:{totalInvestors:new Set(dbData.investments.map((i:any)=>i.userId||i.username)).size,activeInvestments:active.length,totalDeposits:dbData.investments.reduce((n:number,i:any)=>n+Number(i.principal||0),0),totalPrincipal:active.reduce((n:number,i:any)=>n+Number(i.principal||0),0),totalEarningsDistributed:dbData.investment_earnings.reduce((n:number,e:any)=>n+Number(e.amount||0),0),pendingDeposits:dbData.investments.filter((i:any)=>i.status==="Pending").length,pendingWithdrawals:dbData.investment_withdrawals.filter((w:any)=>w.status==="Pending").length,revenuePool:dbData.revenue_pools.filter((p:any)=>p.status==="Approved").reduce((n:number,p:any)=>n+Number(p.amount||0),0)}});});
app.post("/api/v1/admin/revenue-share/plans",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const name=String(req.body?.name||"").trim(),minDeposit=Number(req.body?.minDeposit),maxDeposit=req.body?.maxDeposit==null||req.body?.maxDeposit===""?null:Number(req.body.maxDeposit),share=Number(req.body?.revenueSharePercent),coinRate=Number(req.body?.coinConversionRate);if(!name||!Number.isFinite(minDeposit)||minDeposit<0||(maxDeposit!=null&&(!Number.isFinite(maxDeposit)||maxDeposit<minDeposit))||!Number.isFinite(share)||share<0||!Number.isFinite(coinRate)||coinRate<=0)return res.status(400).json({error:"Invalid plan configuration."});const plan={id:`plan_${Date.now()}_${crypto.randomBytes(3).toString("hex")}`,name,minDeposit,maxDeposit,revenueSharePercent:share,payoutCycle:String(req.body?.payoutCycle||"Monthly"),coinConversionRate:coinRate,status:req.body?.status==="Inactive"?"Inactive":"Active",createdAt:new Date().toISOString()};dbData.investment_plans.push(plan);await persistRevenueRecord("investment_plans",plan);res.status(201).json(plan);});
app.put("/api/v1/admin/revenue-share/plans/:id",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const plan=dbData.investment_plans.find((p:any)=>p.id===req.params.id);if(!plan)return res.status(404).json({error:"Plan not found."});const previous={...plan};for(const k of ['name','payoutCycle','status'])if(req.body?.[k]!=null)plan[k]=k==='status'?(req.body[k]==='Inactive'?'Inactive':'Active'):String(req.body[k]);if(req.body?.minDeposit!=null)plan.minDeposit=Number(req.body.minDeposit);if(req.body?.maxDeposit!==undefined)plan.maxDeposit=req.body.maxDeposit==null||req.body.maxDeposit===""?null:Number(req.body.maxDeposit);if(req.body?.revenueSharePercent!=null)plan.revenueSharePercent=Number(req.body.revenueSharePercent);if(req.body?.coinConversionRate!=null)plan.coinConversionRate=Number(req.body.coinConversionRate);if(!Number.isFinite(plan.minDeposit)||plan.minDeposit<0||(plan.maxDeposit!=null&&(!Number.isFinite(plan.maxDeposit)||plan.maxDeposit<plan.minDeposit))||!Number.isFinite(plan.revenueSharePercent)||plan.revenueSharePercent<0||!Number.isFinite(plan.coinConversionRate)||plan.coinConversionRate<=0){Object.assign(plan,previous);return res.status(400).json({error:"Invalid plan configuration."});}plan.updatedAt=new Date().toISOString();await persistRevenueRecord("investment_plans",plan);res.json(plan);});
app.post("/api/v1/admin/revenue-share/investments/:id/status",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const inv=dbData.investments.find((i:any)=>i.id===req.params.id);if(!inv)return res.status(404).json({error:"Investment not found."});const status=String(req.body?.status||"");if(!['Active','Rejected','Suspended'].includes(status))return res.status(400).json({error:"Unsupported investment status."});inv.status=status;inv.paymentStatus=status==='Active'?'Approved':status;if(status==='Active'&&!inv.startDate)inv.startDate=new Date().toISOString();await persistRevenueRecord("investments",inv);res.json(inv);});
app.post("/api/v1/admin/revenue-share/pools",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const month=String(req.body?.month||"").trim(),amount=Number(req.body?.amount);if(!/^\d{4}-\d{2}$/.test(month)||!Number.isFinite(amount)||amount<=0)return res.status(400).json({error:"Month and revenue amount are required."});const pool={id:`POOL-${month}-${crypto.randomBytes(3).toString("hex")}`,month,amount,distributionDate:req.body?.distributionDate||null,eligiblePlans:Array.isArray(req.body?.eligiblePlans)?req.body.eligiblePlans:[],status:"Draft",createdAt:new Date().toISOString(),distributedAt:null};dbData.revenue_pools.unshift(pool);await persistRevenueRecord("revenue_pools",pool);res.status(201).json(pool);});
app.post("/api/v1/admin/revenue-share/pools/:id/status",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const pool=dbData.revenue_pools.find((p:any)=>p.id===req.params.id);if(!pool)return res.status(404).json({error:"Revenue pool not found."});const status=String(req.body?.status||"");if(!['Draft','Pending Approval','Approved'].includes(status))return res.status(400).json({error:"Unsupported pool status."});pool.status=status;await persistRevenueRecord("revenue_pools",pool);res.json(pool);});
app.post("/api/v1/admin/revenue-share/pools/:id/distribute",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const pool=dbData.revenue_pools.find((p:any)=>p.id===req.params.id);if(!pool)return res.status(404).json({error:"Revenue pool not found."});if(pool.status!=="Approved")return res.status(400).json({error:"Pool must be Approved before distribution."});if(pool.distributedAt)return res.status(409).json({error:"This pool has already been distributed."});const eligible=dbData.investments.filter((i:any)=>i.status==="Active"&&(!pool.eligiblePlans?.length||pool.eligiblePlans.includes(i.planId)));const totalPrincipal=eligible.reduce((n:number,i:any)=>n+Number(i.principal||0),0);if(totalPrincipal<=0)return res.status(400).json({error:"No eligible active investments."});const distributions=[];for(const inv of eligible){const percent=Number(inv.planSnapshot?.revenueSharePercent||0);const gross=Number(pool.amount)*(Number(inv.principal||0)/totalPrincipal);const earning=Math.max(0,Math.round(gross*(percent/100)*100)/100);if(!earning)continue;inv.totalEarnings=Number(inv.totalEarnings||0)+earning;inv.availableEarnings=Number(inv.availableEarnings||0)+earning;inv.monthlyEarnings=earning;const createdAt=new Date().toISOString();const earningRecord={id:`EARN-${pool.month}-${inv.id}`,investmentId:inv.id,userId:inv.userId,username:inv.username,period:pool.month,amount:earning,currency:"USD",createdAt,status:"Completed",source:`Revenue Pool ${pool.id}`};dbData.investment_earnings.push(earningRecord);const tx={id:`ITX-EARN-${pool.month}-${inv.id}`,type:"monthly_revenue",investmentId:inv.id,userId:inv.userId,username:inv.username,amount:earning,currency:"USD",timestamp:createdAt,status:"Completed",source:pool.id,destination:"available_earnings"};dbData.investment_transactions.unshift(tx);const dist={id:`DIST-${pool.month}-${inv.id}`,poolId:pool.id,investmentId:inv.id,userId:inv.userId,amount:earning,currency:"USD",createdAt,status:"Completed"};dbData.revenue_distributions.push(dist);distributions.push(dist);await persistRevenueRecord("investment_earnings",earningRecord);await persistRevenueRecord("investment_transactions",tx);await persistRevenueRecord("investments",inv);await persistRevenueRecord("revenue_distributions",dist);}pool.status="Distributed";pool.distributedAt=new Date().toISOString();await persistRevenueRecord("revenue_pools",pool);res.json({success:true,pool,distributions});});
app.post("/api/v1/admin/revenue-share/withdrawals/:id/status",async(req,res)=>{if(!requireRevenueAdmin(req,res))return;const w=dbData.investment_withdrawals.find((x:any)=>x.id===req.params.id);if(!w)return res.status(404).json({error:"Withdrawal not found."});const next=String(req.body?.status||"");if(!['Approved','Processing','Completed','Rejected','Cancelled'].includes(next))return res.status(400).json({error:"Unsupported withdrawal status."});const prev=w.status;w.status=next;w.adminAction={by:revenueShareAdminEmail(req),at:new Date().toISOString()};if(next==='Completed')w.completionDate=new Date().toISOString();if((next==='Rejected'||next==='Cancelled')&&w.reserved&&prev!=='Rejected'&&prev!=='Cancelled'){const own=dbData.investments.filter((i:any)=>String(i.userId)===String(w.userId)&&i.status==='Active');let rem=Number(w.amount||0);for(const inv of own){if(rem<=0)break;const field=w.type==='earnings'?'availableEarnings':'principal';inv[field]=Number(inv[field]||0)+rem;if(w.type==='principal')inv.activeBalance=Number(inv.activeBalance||0)+rem;rem=0;await persistRevenueRecord('investments',inv);}w.reserved=false;}if(next==='Completed'&&w.payoutFormat==='coins'){const ownInv=dbData.investments.find((i:any)=>i.id===w.investmentId);const rate=Number(ownInv?.planSnapshot?.coinConversionRate||100);const user=dbData.users?.find((u:any)=>String(u.uid)===String(w.userId)||String(u.uniqueId)===String(w.userId)||String(u.username).toLowerCase()===String(w.username).toLowerCase());if(user){user.coins=Number(user.coins||0)+Math.round(Number(w.amount)*rate);await persistUserDurably(user);}}await persistRevenueRecord('investment_withdrawals',w);res.json(w);});

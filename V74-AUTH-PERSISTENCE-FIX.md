# V75 Permanent Session & DP Fix

Refresh does not log the account out. The durable local profile keeps the UI authenticated while the backend session is silently refreshed. User-selected profile photos remain authoritative until the user explicitly selects another photo.

# Solo / 1v1 UI Control Update

- Removed the direct Cover control from the bottom broadcaster rail.
- Cover photo remains available through the existing More menu only.
- Added the broadcaster Camera ON/OFF control to the former Cover position.
- Camera control toggles `userLiveCam` and uses the existing Agora camera state.
- Removed the visible "One Versus One Lobby" wording from the 1v1 header and replaced the action with a compact `1v1` / `PK` action while preserving the existing PK request flow.
- Removed the duplicate top-right PK camera control so there is one camera control location.
- Existing Party/Guest/PK functionality is otherwise untouched.
